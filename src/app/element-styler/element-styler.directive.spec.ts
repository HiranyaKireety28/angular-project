import { ElementStylerDirective } from './element-styler.directive';

describe('ElementStylerDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementStylerDirective();
    expect(directive).toBeTruthy();
  });
});
